import { SCREEN_WIDTH } from '@src/utils/pixelScaling';

// Shared layout constants for Opinion screens
export const OPINION_ITEM_WIDTH = SCREEN_WIDTH;
export const OPINION_ITEM_HEIGHT = (SCREEN_WIDTH * 5) / 4;
export const OPINION_IMAGE_SIZE = 80;
